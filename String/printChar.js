// write a program that will print every character 

function printCharacter(str){
    for(let i=0;i<str.length;i++){
        console.log(str[i]);
    }
    return
}

let str = "Dheeraj";

printCharacter(str);