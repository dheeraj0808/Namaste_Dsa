// write a program to return the length of the liked list

function length(head) {
    let curr = head;
    let count = 0;
    while (curr != null) {

        count++
        // fist increase the count 
        curr = curr.next;
        // then update the value

    }
    return count;
}